-- Циклы в LuaU

-- Что такое цикл?
-- Цикл — это конструкция, которая позволяет выполнять один и тот же блок кода несколько раз подряд.
-- В LuaU есть три основных типа циклов: while, repeat...until и for.

--------------------------------------------------------------
--------------------------------------------------------------

-- 1. Цикл while
-- Синтаксис:
-- while <условие> do
--     <тело цикла>
-- end

-- Пример:
-- Выведем числа от 1 до 5

local i = 1
while i <= 5 do
    print("Шаг:", i)
    i = i + 1
end

-- Как работает:
-- Переменная i увеличивается на каждом шаге.
-- Цикл выполняется, пока i не станет больше 5.

--------------------------------------------------------------
--------------------------------------------------------------

-- 2. Цикл repeat...until
-- Синтаксис:
-- repeat
--     <тело цикла>
-- until <условие>

-- Пример:
-- Выведем числа от 1 до 3

local count = 1
repeat
    print("Счётчик:", count)
    count = count + 1
until count > 3

-- Как работает:
-- Такой цикл всегда выполняется хотя бы один раз.
-- Проверка условия происходит в конце.

--------------------------------------------------------------
--------------------------------------------------------------

-- 3. Цикл for
-- Синтаксис:
-- for <переменная> = <начало>, <конец>, <шаг> do
--     <тело цикла>
-- end

-- Пример:
-- Выведем числа от 1 до 4

for j = 1, 4 do
    print("j =", j)
end

-- Как работает:
-- j принимает значения от 1 до 4 включительно.
-- Шаг по умолчанию равен 1.

--------------------------------------------------------------
--------------------------------------------------------------

-- 4. Цикл for с отрицательным шагом
-- Пример:
-- Выведем числа от 5 до 1 в обратном порядке

for k = 5, 1, -1 do
    print("k =", k)
end

-- Как работает:
-- k уменьшается на 1 на каждом шаге, пока не станет меньше 1.

--------------------------------------------------------------
--------------------------------------------------------------

-- Где применяются циклы в Roblox?
-- - Перебор всех игроков в игре: for _, player in ipairs(Players:GetPlayers()) do ... end
-- - Создание нескольких объектов (монет, врагов и т.д.)
-- - Проверка состояния объектов в Workspace
-- - Анимация, таймеры, массовые действия

--------------------------------------------------------------
--------------------------------------------------------------

-- Задачи на циклы

--------------------------------------------------------------
--------------------------------------------------------------

-- 1. Обратный отсчёт
-- Задача:
-- Выведи в консоль числа от 10 до 1 в обратном порядке.

-- Решение:
for n = 10, 1, -1 do
    print(n)
end

--------------------------------------------------------------
--------------------------------------------------------------

-- 2. Сумма чисел от 1 до 100
-- Задача:
-- Посчитай сумму всех чисел от 1 до 100 и выведи результат.

-- Решение:
local sum = 0
for m = 1, 100 do
    sum = sum + m
end
print("Сумма от 1 до 100:", sum)

--------------------------------------------------------------
--------------------------------------------------------------

-- 3. Перебор игроков в игре (пример для Roblox)
-- Задача:
-- Выведи имена всех игроков в Output.

-- Решение:
local Players = game:GetService("Players")
for _, player in ipairs(Players:GetPlayers()) do
    print("Имя игрока:", player.Name)
end

--------------------------------------------------------------
--------------------------------------------------------------

-- 4. Таблица квадратов чисел
-- Задача:
-- Создай таблицу, где ключ — число от 1 до 5, а значение — его квадрат.
-- Выведи таблицу в консоль.

-- Решение:
local squares = {}
for x = 1, 5 do
    squares[x] = x * x
end
for key, value in pairs(squares) do
    print(key, "в квадрате =", value)
end

--------------------------------------------------------------
--------------------------------------------------------------

-- Практическое задание:
-- Напиши цикл, который выведет только чётные числа от 2 до 20 включительно.
-- Попробуй реализовать это с помощью for.
--------------------------------------------------------------
--------------------------------------------------------------

-- 🎈 Проект 1: "Падающие шарики" — Цикл for
-- Цель: Создать серию шариков, которые появляются в воздухе и падают вниз.

-- 🔧 Шаги:
-- Создайте скрипт:

-- Вставьте новый скрипт в ServerScriptService.

-- Код скрипта:


-- -- Создание 10 шариков, которые падают вниз
-- for i = 1, 10 do
--     -- Создаем новый Part
--     local ball = Instance.new("Part")
--     ball.Shape = Enum.PartType.Ball
--     ball.Size = Vector3.new(2, 2, 2)
--     ball.Position = Vector3.new(math.random(-20, 20), 50, math.random(-20, 20))
--     ball.Anchored = false
--     ball.BrickColor = BrickColor.Random()
--     ball.Parent = workspace
    
--     -- Задержка между созданием шариков
--     wait(0.5)
-- end
-- 🧠 Объяснение:
-- Используется цикл for для создания 10 шариков.

-- Каждый шарик создаётся с случайной позицией по X и Z, на высоте Y = 50.

-- wait(0.5) добавляет задержку между созданием каждого шарика для наглядности.
--------------------------------------------------------------
--------------------------------------------------------------


--  Проект 2: "Открывающаяся дверь" — Цикл while
-- Цель: Создать дверь, которая постепенно открывается при нажатии на кнопку.

-- 🔧 Шаги:
-- Создайте объекты:

-- В Workspace создайте два объекта:

-- Door (Part) — дверь.

-- OpenButton (Part) — кнопка для открытия двери.

-- Создайте скрипт:

-- Вставьте новый скрипт в OpenButton.

-- Код скрипта:

-- local door = workspace:WaitForChild("Door")
-- local button = script.Parent
-- local isOpening = false

-- button.Touched:Connect(function(hit)
--     if not isOpening then
--         isOpening = true
--         local angle = 0
--         while angle < 90 do
--             angle = angle + 5
--             door.CFrame = door.CFrame * CFrame.Angles(0, math.rad(5), 0)
--             wait(0.05)
--         end
--     end
-- end)


-- 🧠 Объяснение:
-- При касании кнопки запускается цикл while, который поворачивает дверь на 90 градусов по оси Y.

-- Переменная isOpening предотвращает повторное открытие двери во время анимации.

-- 🧱 Проект 3: "Построение лестницы" — Цикл repeat...until
-- Цель: Построить лестницу из блоков, используя цикл repeat...until.

-- 🔧 Шаги:
-- Создайте скрипт:

-- Вставьте новый скрипт в ServerScriptService.

-- Код скрипта:
-- local steps = 0
-- local maxSteps = 10
-- local startPosition = Vector3.new(0, 1, 0)

-- repeat
--     steps = steps + 1
--     local step = Instance.new("Part")
--     step.Size = Vector3.new(4, 1, 2)
--     step.Position = startPosition + Vector3.new(0, steps, steps * 2)
--     step.Anchored = true
--     step.BrickColor = BrickColor.new("Bright yellow")
--     step.Parent = workspace
--     wait(0.2)
-- until steps >= maxSteps

-- 🧠 Объяснение:
-- Цикл repeat...until создаёт 10 ступенек, каждая из которых выше и дальше предыдущей, формируя лестницу.

-- wait(0.2) добавляет задержку между созданием ступенек для визуального эффекта.

--------------------------------------------------------------
--------------------------------------------------------------

-- 🎯 Практическое задание
-- Задача: Создайте в Workspace объект Target (Part) и напишите скрипт, который при касании этого объекта создаёт 5 кубиков, вылетающих вверх с задержкой в 0.3 секунды между каждым.

-- Подсказка:

-- Используйте цикл for для создания кубиков.

-- Примените силу к каждому кубику для имитации вылета вверх.

-- Добавьте задержку wait(0.3) между созданием кубиков.

-- 🎯 Реализация задания
-- 📌 Шаг 1: Подготовка объектов
-- Создайте объект Target:

-- Вставьте новый Part в Workspace.

-- Назовите его Target.

-- Убедитесь, что у него установлены свойства:

-- Anchored = true (чтобы он не двигался).

-- CanTouch = true (для срабатывания события касания).

-- Создайте скрипт:

-- Вставьте новый Script внутрь объекта Target.

-- 🧾 Шаг 2: Код скрипта

-- Получаем ссылку на объект Target


-- local target = script.Parent

-- -- Флаг для предотвращения повторного срабатывания
-- local isActivated = false

-- -- Функция, вызываемая при касании
-- local function onTouched(otherPart)
--     -- Проверяем, не был ли уже активирован скрипт
--     if isActivated then return end
--     isActivated = true

--     -- Проверяем, является ли касающийся объект частью персонажа игрока
--     local character = otherPart.Parent
--     if not character or not character:FindFirstChild("Humanoid") then
--         return
--     end

--     -- Создаём 5 кубиков
--     for i = 1, 5 do
--         -- Создаём новый Part
--         local cube = Instance.new("Part")
--         cube.Size = Vector3.new(2, 2, 2) -- Размер кубика
--         cube.Position = target.Position + Vector3.new(0, 5, 0) -- Позиция над Target
--         cube.Anchored = false -- Позволяет физике воздействовать на объект
--         cube.BrickColor = BrickColor.Random() -- Случайный цвет
--         cube.Parent = workspace -- Добавляем в Workspace

--         -- Применяем силу вверх
--         local force = Instance.new("BodyVelocity")
--         force.Velocity = Vector3.new(0, 50, 0) -- Направление и сила
--         force.MaxForce = Vector3.new(0, math.huge, 0) -- Максимальная сила по оси Y
--         force.Parent = cube

--         -- Удаляем силу через 0.5 секунды, чтобы кубик начал падать
--         task.delay(0.5, function()
--             force:Destroy()
--         end)

--         -- Задержка между созданием кубиков
--         task.wait(0.3)
--     end
-- end

-- -- Подключаем функцию к событию касания
-- target.Touched:Connect(onTouched)


-- 🧠 Пояснение:
-- isActivated: предотвращает повторное срабатывание скрипта при множественных касаниях.

-- Проверка Humanoid: обеспечивает, что скрипт срабатывает только при касании игроком.

-- BodyVelocity: применяется для придания кубику начальной скорости вверх.

-- task.delay: используется для удаления силы через 0.5 секунды, позволяя кубику начать падать под действием гравитации.

-- task.wait(0.3): создаёт задержку между созданием каждого кубика.